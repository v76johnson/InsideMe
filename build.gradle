// Rule 4: Root-level configuration file remains empty of application code and repository overrides.
// Central repository settings are safely handled exclusively inside settings.gradle.
plugins {
    id 'com.android.application' version '9.1.1' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.22' apply false
}
