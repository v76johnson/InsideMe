# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
org.gradle.parallel=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
# Required because the project uses AndroidX libraries (Jetpack Compose, androidx.appcompat,
# com.google.android.material, androidx.lifecycle, etc.). Without this, the
# :app:checkDebugAarMetadata task fails with:
#   "Configuration ... contains AndroidX dependencies, but the android.useAndroidX
#    property is not enabled."
android.useAndroidX=true
org.gradle.caching=true
# Configuration cache is disabled for CI stability. AGP 8.7 + the Compose
# compiler plugin can produce spurious configuration-cache misses/failures
# on a fresh CI runner (no prior cache to reuse), which surfaces as confusing
# build errors. Enable locally if you want faster incremental builds.
org.gradle.configuration-cache=false
# Set the maximum number of workers to 4 to avoid overloading the machine.
org.gradle.workers.max=4
# Set the Kotlin compiler execution strategy to in-process to avoid "Could not
# connect to Kotlin compile daemon" error.
kotlin.compiler.execution.strategy=in-process
# Allow the google-services plugin to work even if the google-services.json is not present.
googleServices.missing.passthrough=true
